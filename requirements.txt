bioio>=3.5.0
bioio-nd2>=2.4.0
bioio-ome-tiff>=1.4.0
cellpose>=4.0.0
importlib_metadata>=5.1.0
# 0.18.0 has no abi3 or arm64 wheels, so it cannot install on Python >=3.12 or
# Apple Silicon. 0.19.2 is the first release with both.
itk-elastix>=0.19.2
mahotas>=1.4.13
matplotlib>=3.6.3
nd2reader>=3.3.0
numpy>=1.23.0
packaging>=23.0
pandas>=1.5.2
pystackreg>=0.2.7
# blimp/data.py imports requests and tqdm directly; declared explicitly
# rather than relying on another dependency to happen to supply them.
requests
scikit_image>=0.21.0
SimpleITK>=2.1.0
tomli>=2.0.1
tqdm
typing_extensions>=4.4.0
welford
